linters:
  enable:
    - gofmt
    - goimports
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - misspell
    - gosec
    - prealloc
    - unconvert
    - gocritic
    - gocyclo
    - gocognit
    - dupl
    - lll

linters-settings:
  goimports:
    local-prefixes: github.com/jackielii/structpages

  govet:
    enable:
      - shadow

  gocyclo:
    min-complexity: 15

  gocognit:
    min-complexity: 20

  dupl:
    threshold: 100

  lll:
    line-length: 120

  gocritic:
    enabled-tags:
      - diagnostic
      - experimental
      - opinionated
      - performance
      - style
    disabled-checks:
      - dupImport # https://github.com/go-critic/go-critic/issues/845
      - ifElseChain
      - octalLiteral
      - whyNoLint
      - wrapperFunc

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - dupl
        - gosec
        - gocognit

    - path: examples/
      linters:
        - errcheck
        - gosec
        - dupl

run:
  timeout: 5m
  tests: true

